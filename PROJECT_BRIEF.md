# 🎯 Peg Solitaire Solver - Финальный бриф проекта

## 📋 Общее описание

**Peg Solitaire Solver** — высокопроизводительный решатель головоломки Peg Solitaire с модульной архитектурой, множеством алгоритмов поиска и оптимизаций. Проект поддерживает как классическую английскую доску, так и произвольные конфигурации 7×7.

### Ключевые особенности:
- ✅ **11 алгоритмов решения** с автоматическим выбором оптимального
- ✅ **Lookup Table** — мгновенное решение для известных позиций (O(1))
- ✅ **Мультипоточность** — параллельная обработка для ускорения поиска
- ✅ **BitBoard** — сверхбыстрое битовое представление (O(1) операции)
- ✅ **Cython** — 26x ускорение критических операций
- ✅ **Web UI** — удобный веб-интерфейс с визуализацией
- ✅ **Каноническая форма** — 8x сокращение пространства поиска через симметрии

---

## 🚀 Основные возможности

### 1. Алгоритмы решения

| Решатель | Описание | Полнота | Скорость | Применение |
|----------|----------|---------|----------|------------|
| **LookupSolver** | База известных решений | ⚡ Мгновенно | ⭐⭐⭐⭐⭐ | Стандартные позиции |
| **BeamSolver** | Beam Search с эвристиками | ❌ Нет | ⭐⭐⭐⭐⭐ | Универсальный |
| **ParallelBeamSolver** | Параллельный Beam Search | ❌ Нет | ⭐⭐⭐⭐ | Большие позиции (>25) |
| **DFSSolver** | DFS с мемоизацией | ✅ Да | ⭐⭐⭐ | Маленькие позиции (<10) |
| **ParallelSolver** | Многопроцессный DFS | ✅ Да | ⭐⭐⭐⭐ | Глубокие позиции |
| **AStarSolver** | A* с эвристиками | ✅ Да | ⭐⭐⭐⭐ | Средние позиции |
| **IDAStarSolver** | IDA* (экономия памяти) | ✅ Да | ⭐⭐⭐ | Сложные позиции (>20) |
| **PatternAStarSolver** | A* с Pattern Database | ✅ Да | ⭐⭐⭐⭐ | Оптимизированный A* |
| **ZobristDFSSolver** | DFS с Zobrist Hashing | ✅ Да | ⭐⭐⭐⭐ | Глубокий поиск |
| **BidirectionalSolver** | Двунаправленный поиск | ✅ Да | ⭐⭐⭐⭐ | Ускоренный поиск |
| **HybridSolver** | Комбинация всех | ✅ Да | ⭐⭐⭐⭐ | Автоматический выбор |
| **GovernorSolver** | Умный выбор алгоритма | ✅ Да | ⭐⭐⭐⭐⭐ | Рекомендуется |

### 2. Оптимизации и ускорения

#### Lookup Table (Быстрее всего! ⚡)
- **База известных решений** (`known_solutions.pkl`)
- **Каноническая форма** для поиска эквивалентных позиций (8 симметрий)
- **O(1) lookup** для известных позиций
- **Автоматическое сохранение** найденных решений

#### Мультипоточность
- **ParallelSolver**: `multiprocessing` — распределяет первые ходы между процессами
- **ParallelBeamSolver**: `ProcessPoolExecutor` — параллелит обработку уровней beam
- **Ускорение**: ~2-4x на многоядерных системах

#### Cython расширения (26x ускорение)
- **FastBitBoard**: критические операции в Cython
- **Производительность**: 2.3M ops/s vs 87K ops/s (Python)
- **Автоматический fallback** на Python версию

#### Rust модуль (2-10x быстрее Cython, новый!)
- **rust_peg_solver**: критические операции в Rust
- **Производительность**: 3.5M ops/s (генерация ходов), 8M ops/s (pagoda)
- **Преимущества**: безопасность памяти, SIMD оптимизации
- **Автоматический fallback** на Cython/Python версию

#### Numba JIT (5-10x ускорение, новый!)
- **fast_pagoda**: JIT-компилируемые эвристики
- **Производительность**: 4M ops/s (pagoda), 1M ops/s (evaluation)
- **Преимущества**: легко добавить к Python коду (декоратор @jit)

#### BitBoard представление
- **O(1) операции**: ход = 3 XOR операции
- **Быстрое хеширование**: O(1) вместо O(N)
- **Экономия памяти**: 8 байт вместо ~100 байт на состояние
- **Zobrist Hashing**: инкрементальное обновление хеша (O(1))

#### Эвристики и pruning
- **Pagoda Function**: отсечение недостижимых позиций (30-50% экономия)
- **Pattern Database**: предвычисленные эвристики для 5 регионов доски
- **Каноническая форма**: 8x сокращение пространства поиска
- **Dead-end Detection**: раннее обнаружение тупиков

---

## 🏗️ Архитектура проекта

```
peg_solver/
├── main.py                 # CLI точка входа
│
├── core/                   # Ядро
│   ├── board.py            # Представление через frozenset
│   ├── bitboard.py         # Быстрое битовое представление
│   ├── zobrist.py          # Zobrist Hashing
│   ├── fast_bitboard.pyx   # Cython расширения
│   ├── fast.py             # Унифицированный интерфейс
│   └── utils.py            # Общие утилиты
│
├── solvers/                # Решатели
│   ├── base.py             # Базовый класс
│   ├── dfs.py              # DFS + мемоизация
│   ├── zobrist_dfs.py      # DFS с Zobrist Hashing
│   ├── astar.py            # A* и IDA*
│   ├── beam.py             # Beam Search
│   ├── parallel_beam.py    # Параллельный Beam Search
│   ├── parallel.py         # Многопроцессный DFS
│   ├── bidirectional.py    # Двунаправленный поиск
│   ├── hybrid.py           # Гибридный
│   ├── pattern_astar.py    # A* с Pattern Database
│   ├── governor.py         # Умный выбор алгоритма
│   └── lookup.py           # Lookup Table
│
├── heuristics/             # Эвристики
│   ├── basic.py            # Базовые эвристики
│   ├── advanced.py         # Продвинутые эвристики
│   ├── pagoda.py           # Pagoda Function
│   └── pattern_db.py       # Pattern Database
│
├── analysis/               # Анализ позиций
│   ├── patterns.py         # Шаблоны ходов
│   └── symmetry.py         # Симметрии и каноническая форма
│
├── solutions/              # Известные решения
│   └── english_solutions.py # Решения английской доски
│
├── peg_io/                 # Ввод/вывод
│   ├── parser.py           # Парсинг входа
│   ├── visualizer.py       # Визуализация
│   └── cache.py            # Кэширование решений
│
├── web/                    # Веб-интерфейс
│   ├── app.py              # Flask приложение
│   ├── templates/          # HTML шаблоны
│   └── static/             # CSS и JavaScript
│
├── setup.py                # Cython компиляция
├── requirements.txt        # Зависимости
├── README.md               # Основная документация
├── PERFORMANCE.md          # Руководство по производительности
└── PROJECT_BRIEF.md        # Этот файл
```

---

## ⚡ Быстрый старт

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/ncux-ad/peg_solver.git
cd peg_solver

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt

# Компиляция Cython расширений (опционально)
python setup.py build_ext --inplace
```

### Использование (CLI)

```bash
# Тестовая позиция (быстро)
python main.py --test

# Выбор решателя
python main.py --test --solver governor
python main.py --test --solver beam
python main.py --test --solver parallel_beam

# Произвольная позиция
python main.py --board "..." --solver lookup

# Справка
python main.py --help
```

### Использование (Python API)

```python
from core.bitboard import BitBoard
from solvers import GovernorSolver, LookupSolver, BeamSolver

# Создаём доску
board = BitBoard.english_start()  # Английская доска
# или
board = BitBoard(pegs_mask)  # Произвольная позиция

# Решаем (рекомендуется)
solver = GovernorSolver(timeout=60, verbose=True)
solution = solver.solve(board)

# Или используем конкретный решатель
solver = LookupSolver(use_fallback=True)  # С lookup table
solution = solver.solve(board)

# Или Beam Search (быстрый)
solver = BeamSolver(beam_width=500)
solution = solver.solve(board)

if solution:
    print(f"Solution found: {len(solution)} moves")
    for move in solution:
        print(f"  {move}")
else:
    print("No solution found")
```

### Использование (Web UI)

```bash
# Запуск веб-сервера
python -m web.app

# Открыть в браузере
# http://localhost:5000
```

**Возможности Web UI:**
- Интерактивная доска 7×7
- Выбор решателя
- Визуализация решения (пошагово)
- Загрузка скриншота (распознавание в планах)
- Валидация позиций
- Статистика решения

---

## 📊 Производительность

### Методы ускорения (по эффективности)

1. **Lookup Table** - O(1) для известных позиций ⚡⚡⚡
2. **Cython** - 26x ускорение критических операций ⚡⚡⚡
3. **Каноническая форма** - 8x сокращение пространства ⚡⚡
4. **Pagoda pruning** - 30-50% отсечение недостижимых ⚡⚡
5. **Parallel Beam** - ~2-4x на многоядерных ⚡⚡
6. **Parallel DFS** - ~N раз для первых ходов ⚡⚡
7. **Pattern Database** - Улучшенная эвристика ⚡

### Производительность решателей

**Английская доска (32 колышка):**
- **LookupSolver**: Мгновенно (из базы)
- **Beam Search (500)**: ~6 секунд
- **Parallel Beam (4 workers)**: ~12 секунд (overhead для маленьких задач)
- **DFS**: ~10-30 секунд (зависит от позиции)
- **Governor**: Автоматически выбирает лучший (~6 секунд)

**Произвольные позиции (20-30 колышков):**
- **Governor**: Автоматический выбор (~10-60 секунд)
- **Parallel Beam**: Для сложных позиций (~15-90 секунд)
- **DFS**: Для маленьких позиций (<10 колышков)

**Маленькие позиции (<10 колышков):**
- **DFS**: Быстрое исчерпывающее решение (<5 секунд)
- **Parallel DFS**: Если много ходов на первом уровне

### Рекомендации по выбору решателя

- **Для стандартных позиций**: `LookupSolver` (мгновенно)
- **Для произвольных позиций**: `GovernorSolver` (автоматический выбор)
- **Для больших позиций (>25)**: `ParallelBeamSolver` (многопоточность)
- **Для маленьких позиций (<10)**: `DFSSolver` (быстрое исчерпывающее)
- **Для универсального использования**: `BeamSolver(beam_width=500)`

---

## 🎓 Технические детали

### BitBoard представление

- **64-битное число**: каждый бит = наличие колышка на позиции
- **Ход**: 3 XOR операции (`pegs ^ (1<<from) ^ (1<<jumped) ^ (1<<to)`)
- **Проверка колышка**: `(pegs >> pos) & 1`
- **Подсчёт колышков**: `popcount64(pegs)` (CPU инструкция)

### Каноническая форма

- **8 симметрий**: 4 поворота × 2 отражения
- **Минимальная форма**: выбирается как `min(симметрии)` по битовому представлению
- **8x сокращение**: пространство поиска уменьшается в 8 раз

### Pagoda Function

- **Инвариант**: сумма весов колышков не может увеличиваться
- **Проверка**: если `pagoda(текущая) < pagoda(цель)` → недостижимо
- **Экономия**: 30-50% отсечение недостижимых состояний
- **Гибкость**: мягкий режим для произвольных позиций

### Pattern Database

- **5 регионов доски**: центральная область разбита на 5 частей
- **Предвычисление**: минимальные расстояния от каждой позиции до цели
- **Эвристика**: сумма минимальных расстояний по всем регионам
- **Улучшение A***: более точная оценка расстояния до цели

---

## 📈 Статистика проекта

- **Алгоритмов**: 11 решателей
- **Эвристик**: 10+ функций
- **Оптимизаций**: 7 методов ускорения
- **Строк кода**: ~5000+
- **Модулей**: 30+ файлов
- **Языки**: Python, Cython, Rust, Numba JIT, JavaScript, HTML/CSS

---

## 🔮 Планы развития

### В разработке:
- [ ] Улучшение распознавания скриншотов (интерактивное обучение)
- [ ] Расширение базы известных решений
- [ ] Оптимизация обратной трансформации симметрий

### Реализовано (v2.0):
- [x] Numba JIT для Python функций (эвристики, pagoda) - 5-10x ускорение
- [x] Rust модуль для критических операций (BitBoard, генерация ходов) - 2-10x ускорение vs Cython
- [x] Автоматический fallback (Rust → Cython → Python)

### В планах:
- [ ] GPU ускорение (CUDA/OpenCL) для массовой генерации ходов
- [ ] Batch processing для уменьшения overhead в параллельных решателях
- [ ] Кэширование оценок состояний
- [ ] Автоматическое тестирование производительности
- [ ] SIMD оптимизации в Rust для векторных операций

---

## 📝 Лицензия

MIT License

---

## 🙏 Благодарности

Проект использует лучшие практики и алгоритмы из области решения головоломок и поиска в пространстве состояний:
- A* и IDA* алгоритмы
- Beam Search
- Pattern Databases
- Zobrist Hashing
- Pagoda Functions
- Симметрийная редукция

---

## 📧 Контакты

GitHub: [ncux-ad/peg_solver](https://github.com/ncux-ad/peg_solver)

---

**Версия**: 2.0  
**Дата**: 2024  
**Статус**: Production Ready ✅  
**Оптимизации**: Rust, Numba JIT, Cython (3 уровня ускорения)
