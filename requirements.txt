# Peg Solitaire Solver Requirements
# ===================================

# Основные зависимости
# Проект использует только стандартную библиотеку Python 3.8+

# ===================================
# Для сборки Cython расширений (ускорение ~26x)
# ===================================

Cython>=3.0.0
setuptools>=80.0.0
wheel>=0.45.0

# ===================================
# Для разработки и тестирования
# ===================================

pytest>=9.0.0
iniconfig>=2.0.0
pluggy>=1.0.0
packaging>=24.0
Pygments>=2.0.0

# ===================================
# Установка
# ===================================
#
# 1. Создать venv:
#    python -m venv venv
#    source venv/bin/activate  # Linux/Mac
#    venv\Scripts\activate     # Windows
#
# 2. Установить зависимости:
#    pip install -r requirements.txt
#
# 3. Скомпилировать Cython (опционально, для ускорения):
#    python setup.py build_ext --inplace
#
# 4. Проверить:
#    python -c "from core.fast import USING_CYTHON; print(f'Cython: {USING_CYTHON}')"
