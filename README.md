# Peg Solitaire Solver

🎯 Высокопроизводительный решатель головоломки Peg Solitaire с модульной архитектурой.

## Возможности

- **6 алгоритмов решения**: DFS, A*, IDA*, Beam Search, Parallel, Bidirectional
- **BitBoard представление**: O(1) операции через битовую арифметику
- **Pagoda function pruning**: отсечение недостижимых состояний
- **Symmetry reduction**: 8x сокращение пространства поиска
- **Модульная архитектура**: легко расширять и отлаживать

## Быстрый старт

```bash
# Тестовая позиция (быстро)
python main.py --test

# Выбор решателя
python main.py --test --solver dfs
python main.py --test --solver beam
python main.py --test --solver parallel

# Справка
python main.py --help
```

## Структура проекта

```
peg_solver/
├── main.py                 # CLI точка входа
│
├── core/                   # Ядро
│   ├── board.py            # Представление через frozenset
│   ├── bitboard.py         # Быстрое битовое представление
│   └── utils.py            # Общие утилиты
│
├── solvers/                # Решатели (каждый в своём файле)
│   ├── base.py             # Базовый класс
│   ├── dfs.py              # DFS + мемоизация
│   ├── astar.py            # A* и IDA*
│   ├── beam.py             # Beam Search
│   ├── parallel.py         # Многопоточный DFS
│   ├── bidirectional.py    # Двунаправленный поиск
│   └── hybrid.py           # Гибридный (комбинирует все)
│
├── heuristics/             # Эвристики
│   ├── basic.py            # Базовые
│   ├── pagoda.py           # Pagoda function
│   └── advanced.py         # Продвинутые
│
├── analysis/               # Анализ позиций
│   ├── patterns.py         # Шаблоны ходов
│   └── symmetry.py         # Симметрии
│
└── peg_io/                 # Ввод/вывод
    ├── parser.py           # Парсинг входа
    ├── visualizer.py       # Визуализация
    └── cache.py            # Кэширование решений
```

## Решатели

| Решатель | Описание | Полнота | Скорость |
|----------|----------|---------|----------|
| `dfs` | DFS с мемоизацией | ✅ Да | ⭐⭐⭐ |
| `astar` | A* с эвристиками | ✅ Да | ⭐⭐⭐⭐ |
| `ida` | IDA* (экономия памяти) | ✅ Да | ⭐⭐⭐ |
| `beam` | Beam Search | ❌ Нет | ⭐⭐⭐⭐⭐ |
| `parallel` | Многопоточный DFS | ✅ Да | ⭐⭐⭐⭐ |
| `hybrid` | Комбинация всех | ✅ Да | ⭐⭐⭐⭐ |

## Использование как библиотеки

```python
from core.bitboard import BitBoard
from solvers import DFSSolver, HybridSolver

# Создаём доску
board = BitBoard.english_start()

# Решаем
solver = DFSSolver(verbose=True)
solution = solver.solve(board)

# Или гибридный подход
solver = HybridSolver(timeout=60)
solution = solver.solve(board)
```

## Эвристики

```python
from heuristics import pagoda_value, PAGODA_WEIGHTS
from core.bitboard import BitBoard

board = BitBoard.english_start()

# Проверка решаемости
if pagoda_value(board) >= PAGODA_WEIGHTS[24]:
    print("Позиция может быть решена")
```

## Требования

- Python 3.8+
- Стандартная библиотека (без внешних зависимостей)

## Лицензия

MIT
