# Обратный поиск (Backward Search) для Peg Solitaire

## 📚 Теоретическая основа

Обратный поиск — один из самых эффективных методов для решения Peg Solitaire на произвольных досках. Вместо того чтобы начинать с исходной позиции и пытаться уменьшить число колышков, алгоритм начинает с **целевого состояния** (например, один колышек) и "разворачивает" ходы назад.

## 🎯 Преимущества обратного поиска

1. **Меньше ветвление**: В начале обратного поиска (1 колышек) меньше возможных ходов, чем в начале прямого поиска (много колышков)
2. **Более предсказуемое пространство поиска**: Легче контролировать рост состояний
3. **Эффективен для произвольных досок**: Не требует специальных эвристик для конкретной топологии

## 🔄 Как работает обратный ход

### Прямой ход
```
(from, jumped, to): 
- Удаляет колышек из `from`
- Удаляет колышек из `jumped`  
- Создаёт колышек в `to`
```

### Обратный ход
```
(to, jumped, from):
- Создаёт колышек в `from`
- Создаёт колышек в `jumped`
- Удаляет колышек из `to`
```

**Важно**: Правила симметричны — если можно прыгнуть из A через B в C, то в обратном направлении можно "создать" колышки в A и B, убрав C.

## 💻 Реализация BackwardSimpleSolver

### Алгоритм

1. **Инициализация**: Начинаем с целевого состояния (1 колышек)
2. **BFS поиск**: Используем поиск в ширину от целевого к начальному состоянию
3. **Генерация обратных ходов**: Для каждой занятой ячейки ищем возможные обратные ходы
4. **Инверсия пути**: Когда находим путь к начальному состоянию, инвертируем ходы для получения решения

### Ключевые методы

#### `_generate_reverse_moves(board)`
Генерирует все возможные обратные ходы для текущего состояния:
- Для каждой занятой ячейки (to)
- Проверяет все направления (вверх, вниз, влево, вправо)
- Обратный ход возможен, если:
  - `to` занята (будет удалена)
  - `jumped` свободна (будет создана)
  - `from` свободна (будет создана)

#### `_apply_reverse_move(board, reverse_move)`
Применяет обратный ход:
- Создаёт колышки в `from` и `jumped`
- Удаляет колышек из `to`

#### `_invert_path(reverse_path)`
Инвертирует путь обратных ходов в прямой путь для получения решения.

## 📊 Сравнение с другими методами

| Метод | Ветвление в начале | Память | Скорость | Применимость |
|-------|-------------------|--------|----------|--------------|
| **Прямой DFS** | Высокое (много колышков) | Средняя | Медленная | Универсальная |
| **Обратный поиск** | Низкое (1 колышек) | Средняя | Быстрая | ✅ Произвольные доски |
| **Bidirectional** | Среднее (с двух сторон) | Высокая | Средняя | Универсальная |
| **IDA*** | Высокое | Низкая | Медленная | С хорошими эвристиками |

## 🚀 Использование

```python
from solvers.backward_simple import BackwardSimpleSolver
from core.bitboard import BitBoard

# Создаём решатель
solver = BackwardSimpleSolver(verbose=True)

# Решаем доску
board = BitBoard(...)  # Начальная позиция
solution = solver.solve(board)

if solution:
    print(f"Решение найдено: {len(solution)} ходов")
    for move in solution:
        print(f"{move[0]} -> {move[2]} (через {move[1]})")
```

## ⚠️ Ограничения и оптимизации

### Текущие ограничения

1. **Большие доски**: Может работать медленно на очень больших досках (>30 колышков)
2. **Память**: BFS требует хранения всех посещённых состояний
3. **Нет эвристик**: Не использует эвристики для направления поиска

### Возможные оптимизации

1. **IDA* для обратного поиска**: Использовать итеративное углубление вместо BFS
2. **Эвристики**: Добавить эвристики для оценки расстояния до начального состояния
3. **Resource Counting**: Использовать подсчёт ресурсов для отсечения ветвей
4. **Симметрии**: Использовать канонические формы для уменьшения пространства поиска

## 📈 Результаты

### Тестирование на произвольных досках

Для доски с 29 колышками:
- **BackwardSimpleSolver**: ⏳ Требует оптимизации (работает, но медленно)
- **DFSMemoSolver**: ~105 секунд
- **BeamSimpleSolver**: ~14 секунд

### Рекомендации

- ✅ Использовать для маленьких/средних досок (<25 колышков)
- ⚠️ Для больших досок использовать BeamSimpleSolver или оптимизированные решатели
- 🔄 Комбинировать с другими методами (например, в SequentialSolver)

## 🔗 Связанные алгоритмы

- **BidirectionalSimpleSolver**: Двунаправленный поиск (комбинация прямого и обратного)
- **DFSMemoSolver**: Прямой поиск с мемоизацией
- **BeamSimpleSolver**: Быстрый неполный поиск

## 📚 Литература

- **John D. Beasley**, *The Ins and Outs of Peg Solitaire* — классическая книга по солитеру
- **George I. Bell** — исследования по обобщённому солитеру и resource counting
- **Обратный поиск** — стандартный метод в комбинаторных задачах
