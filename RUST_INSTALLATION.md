# 🦀 Установка Rust для Peg Solitaire Solver

## 🚀 Быстрая установка (рекомендуется)

**Используйте наглядный скрипт установки:**

```bash
./install_rust.sh
```

Этот скрипт:
- ✅ Показывает прогресс установки в реальном времени
- ✅ Отображает все шаги с понятными сообщениями
- ✅ Проверяет установку автоматически
- ✅ Даёт подсказки по следующим шагам

## ⏱️ Процесс установки

Установка Rust через `rustup` обычно занимает **3-10 минут** в зависимости от скорости интернета.

### Что скачивается:
- rustup (менеджер инструментов) - ~2 МБ
- rustc (компилятор) - ~100-150 МБ
- Cargo (менеджер пакетов) - ~50 МБ
- Стандартная библиотека - ~50-100 МБ
- Документация (опционально) - ~200-300 МБ

**Итого**: ~200-500 МБ (в зависимости от компонентов)

## 🔧 Ручная установка (альтернатива)

Если хотите установить вручную:

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

## ✅ После завершения установки

### 1. Активируйте переменные окружения

```bash
# Перезагрузите shell или выполните:
source $HOME/.cargo/env
```

### 2. Проверьте установку

```bash
rustc --version
cargo --version
```

Ожидаемый вывод:
```
rustc 1.70.0 (90c541806 2023-XX-XX)
cargo 1.70.0 (...
```

### 3. Установите maturin (для сборки Python модулей)

```bash
# Активируйте venv (если используете)
source venv/bin/activate  # Linux/Mac

# Установите maturin с увеличенным таймаутом
pip install --timeout=300 --retries=5 maturin
```

### 4. Соберите Rust модуль

```bash
cd rust_peg_solver
./build.sh
# или
maturin develop --release
```

### 5. Проверьте, что Rust модуль работает

```bash
python -c "from core.rust_fast import USING_RUST; print(f'Rust: {USING_RUST}')"
```

Ожидаемый вывод: `Rust: True`

## ⚠️ Важно

**Rust модуль опционален!** Проект работает отлично и без него:

- ✅ **Cython** даёт 26x ускорение (уже есть)
- ✅ **Numba JIT** даёт 5-10x ускорение эвристик (уже есть)
- 🦀 **Rust** даёт дополнительное 2-10x ускорение (опционально)

Если Rust не устанавливается или возникают проблемы - это не критично! Используйте Cython/Numba - они уже дают отличную производительность.

## 🔍 Проверка прогресса установки

Если процесс кажется зависшим, можно проверить:

```bash
# Проверить, идёт ли процесс
ps aux | grep rustup

# Проверить, установился ли Rust
which rustc

# Проверить размер скачанных файлов
du -sh ~/.rustup 2>/dev/null || echo "Ещё скачивается..."
```

## 💡 Альтернатива: Продолжить без Rust

Если установка затягивается и вы хотите начать работать:

```bash
# Проект работает без Rust!
python setup.py build_ext --inplace  # Собрать Cython модуль

# Проверить работоспособность
python -c "
from core.fast import USING_CYTHON
from heuristics.fast_pagoda import NUMBA_AVAILABLE
print(f'Cython: {USING_CYTHON}')
print(f'Numba: {NUMBA_AVAILABLE}')
"
```

Всё будет работать! Rust можно установить позже для максимальной производительности.
