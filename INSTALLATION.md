# 📦 Полная инструкция по установке

## Почему нужны системные зависимости?

Проект использует **два типа зависимостей**:

1. **Python зависимости** (pip) - устанавливаются через `pip install`
2. **Системные зависимости** (apt/yum/dnf) - устанавливаются через системный пакетный менеджер

### Почему системные зависимости не в requirements.txt?

`requirements.txt` может содержать только **Python пакеты**, которые устанавливаются через pip. Системные пакеты (python3-dev, build-essential) устанавливаются через системный пакетный менеджер (apt, yum, dnf, pacman) и требуют прав администратора.

## 🚀 Быстрая установка (рекомендуется)

Используйте единый скрипт, который установит всё автоматически:

```bash
./build_all.sh
```

Скрипт:
- ✅ Проверит наличие системных зависимостей
- ✅ Предложит установить их автоматически
- ✅ Установит все Python зависимости
- ✅ Скомпилирует Cython расширения
- ✅ Установит Numba
- ✅ Опционально установит и соберёт Rust модуль

## 📋 Пошаговая установка

### Шаг 1: Системные зависимости

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install -y python3-dev build-essential
```

**Fedora/RHEL/CentOS:**
```bash
sudo dnf install -y python3-devel gcc gcc-c++ make
```

**Arch Linux:**
```bash
sudo pacman -S --noconfirm python base-devel
```

**Проверка:**
```bash
python3 -c "import sysconfig; print(sysconfig.get_path('include'))"
ls $(python3 -c "import sysconfig; print(sysconfig.get_path('include'))")/Python.h
```

Если `Python.h` найден - всё готово!

### Шаг 2: Python зависимости

```bash
# Создайте виртуальное окружение (рекомендуется)
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt
```

### Шаг 3: Компиляция Cython (опционально, но рекомендуется)

```bash
python setup.py build_ext --inplace
```

**Проверка:**
```bash
python -c "from core.fast import USING_CYTHON; print(f'Cython: {USING_CYTHON}')"
```

### Шаг 4: Rust модуль (опционально, для максимальной производительности)

```bash
# Установите Rust
./install_rust.sh

# Установите maturin
pip install maturin

# Соберите модуль
cd rust_peg_solver
./build.sh
cd ..
```

## ❓ Частые проблемы

### Ошибка: "Python.h: No such file or directory"

**Причина**: Не установлен пакет `python3-dev` (или `python3-devel`)

**Решение**:
```bash
# Ubuntu/Debian
sudo apt-get install python3-dev

# Fedora/RHEL
sudo dnf install python3-devel
```

### Ошибка: "gcc: command not found"

**Причина**: Не установлен компилятор C

**Решение**:
```bash
# Ubuntu/Debian
sudo apt-get install build-essential

# Fedora/RHEL
sudo dnf install gcc gcc-c++ make
```

### Cython компилируется, но не используется

**Проверка**:
```bash
python -c "from core.fast import USING_CYTHON; print(USING_CYTHON)"
```

Если `False`, проверьте:
- Файл `core/fast_bitboard.so` существует
- Нет ошибок импорта в `core/fast.py`

## 📚 Дополнительная информация

- [INSTALL_DEPS.md](INSTALL_DEPS.md) - Детали по системным зависимостям
- [OPTIMIZATION_GUIDE.md](OPTIMIZATION_GUIDE.md) - Руководство по оптимизациям
- [RUST_INSTALLATION.md](RUST_INSTALLATION.md) - Установка Rust
